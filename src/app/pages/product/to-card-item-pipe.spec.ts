import { ToCardItemPipe } from './to-card-item-pipe';

describe('ToCardItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ToCardItemPipe();
    expect(pipe).toBeTruthy();
  });
});
